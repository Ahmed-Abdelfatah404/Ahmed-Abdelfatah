# ============================================================================
# GITHUB ACTIONS AUTO-BUILDER WORKFLOW (.github/workflows/build-mac-app.yml) - v6
# Builds your macOS App and uploads it both to Actions Artifacts & GitHub Releases.
# Optimized to prevent "universal" architecture compilation crashes on GitHub Runners!
# ============================================================================
name: Build and Release macOS App

on:
  push:
    tags:
      - 'v*' # Triggers when you push a version tag (e.g. v1.0.0)
  workflow_dispatch: # Allows you to trigger the build manually from the Actions tab!

jobs:
  release:
    runs-on: macos-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 24

      - name: Initialize Electron App Structure
        run: |
          # Create a clean package.json
          if [ ! -f package.json ]; then
          cat << 'EOF' > package.json
          {
            "name": "ahmed-abdelfatah-app",
            "version": "1.0.0",
            "main": "main.js",
            "scripts": {
              "build": "electron-builder --mac"
            },
            "build": {
              "appId": "com.ahmed.abdelfatah.app",
              "productName": "Ahmed Abd-Elfatah App",
              "mac": {
                "category": "public.app-category.education",
                "target": ["dmg", "zip"]
              }
            }
          }
          EOF
          fi

          # Create main.js
          if [ ! -f main.js ]; then
          cat << 'EOF' > main.js
          const { app, BrowserWindow, session } = require('electron');
          function createWindow() {
              session.defaultSession.webRequest.onBeforeSendHeaders((details, callback) => {
                  details.requestHeaders['User-Agent'] = 'AhmedAcademySecureApp_SecureVideoSessionEngine';
                  callback({ cancel: false, requestHeaders: details.requestHeaders });
              });
              const mainWindow = new BrowserWindow({
                  width: 1280,
                  height: 800,
                  title: 'Ahmed Abd-Elfatah App',
                  autoHideMenuBar: true,
                  webPreferences: {
                      contextIsolation: true,
                      nodeIntegration: false
                  }
              });
              mainWindow.loadURL('https://ahmed-abd-elfatah.pages.dev?app=true');
          }
          app.whenReady().then(() => {
              createWindow();
              app.on('activate', () => {
                  if (BrowserWindow.getAllWindows().length === 0) createWindow();
              });
          });
          app.on('window-all-closed', () => {
              if (process.platform !== 'darwin') app.quit();
          });
          EOF
          fi

      - name: Install Dependencies
        run: |
          npm install electron electron-builder --save-dev

      - name: Build macOS Application
        run: |
          # 🍏 FIX: Removed '--universal' flag. Building standard Mac app to bypass lipo-merge/code-signing crashes.
          npx electron-builder --mac
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          CSC_IDENTITY_AUTO_DISCOVERY: false # Disables Apple certificate lookups to prevent build failure on local machines or VM environments!

      # Save compiled app as a workflow artifact
      - name: Upload Built App to GitHub Actions Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: Ahmed-Abd-Elfatah-Mac-App
          path: |
            dist/*.dmg
            dist/*.zip

      # Upload to GitHub Releases (runs ONLY when triggering via tag v1.0.0)
      - name: Upload Binaries to Release Assets
        uses: softprops/action-gh-release@v2
        if: startsWith(github.ref, 'refs/tags/')
        with:
          files: |
            dist/*.dmg
            dist/*.zip
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
