# ============================================================================
# GITHUB ACTIONS AUTO-BUILDER WORKFLOW (.github/workflows/build-mac-app.yml)
# Automatically builds and compiles your macOS App and uploads it to GitHub Releases!
# ============================================================================
name: Build and Release macOS App

on:
  push:
    tags:
      - 'v*' # Triggers when you push a version tag (e.g. v1.0.0)
  workflow_dispatch: # Allows you to trigger the build manually from the Actions tab!

jobs:
  release:
    runs-on: macos-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'

      - name: Initialize Electron App Structure
        run: |
          # Create package.json if it doesn't exist
          if [ ! -f package.json ]; then
            echo '{
              "name": "ahmed-abd-elfatah-app",
              "version": "1.0.0",
              "main": "main.js",
              "scripts": {
                "build": "electron-builder --mac"
              },
              "build": {
                "appId": "com.ahmed.abdelfatah.app",
                "productName": "Ahmed Abd-Elfatah App",
                "mac": {
                  "category": "public.app-category.education",
                  "target": ["dmg", "zip"]
                }
              }
            }' > package.json
          fi

          # Create main.js if it doesn't exist
          if [ ! -f main.js ]; then
            echo "const { app, BrowserWindow, session } = require('electron');
            function createWindow() {
                session.defaultSession.webRequest.onBeforeSendHeaders((details, callback) => {
                    details.requestHeaders['User-Agent'] = 'AhmedAcademySecureApp_SecureVideoSessionEngine';
                    callback({ cancel: false, requestHeaders: details.requestHeaders });
                });
                const mainWindow = new BrowserWindow({
                    width: 1280,
                    height: 800,
                    title: 'Ahmed Abd-Elfatah App',
                    autoHideMenuBar: true,
                    webPreferences: {
                        contextIsolation: true,
                        nodeIntegration: false
                    }
                });
                mainWindow.loadURL('https://ahmed-abd-elfatah.pages.dev?app=true');
            }
            app.whenReady().then(() => {
                createWindow();
                app.on('activate', () => {
                    if (BrowserWindow.getAllWindows().length === 0) createWindow();
                });
            });
            app.on('window-all-closed', () => {
                if (process.platform !== 'darwin') app.quit();
            });" > main.js
          fi

      - name: Install Dependencies
        run: |
          npm install electron electron-builder --save-dev

      - name: Build macOS Application
        run: |
          npx electron-builder --mac --universal
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Upload Binaries to Release Assets
        uses: softprops/action-gh-release@v2
        if: startsWith(github.ref, 'refs/tags/')
        with:
          files: |
            dist/*.dmg
            dist/*.zip
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
